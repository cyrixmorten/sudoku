package framework;

import toolbox.Coord;

public interface SolverStrategy {

	Board run(Board board, Enum<Board.Areas> area, Coord c);
	
	boolean optionRemoved();
	
	boolean valueSet();
	
	String actionsMade();
	
	int runCount();
	
	Enum<Board.Areas>[] areas();
	
}
