package framework;

import java.util.List;

import toolbox.Coord;
import toolbox.Options;

public interface Tile {

	boolean setValue(int value);
	int getValue();
	Coord getCoordinate();
	void setOptions(List<Integer> options);
	boolean removeOption(int option);
	boolean removeOptions(List<Integer> option);
	Options getOptions();
	String toString();
	boolean equals(Object o);
	
}
